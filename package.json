{
  "name": "learn-gpgpu",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && tsc --noEmit && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "vitest": "vitest"
  },
  "dependencies": {
    "@astrojs/check": "^0.2.0",
    "astro": "^3.3.2",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "vitest": "^1.5.2"
  }
}
